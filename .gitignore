.venv/
__pycache__/
*.pyc
.pytest_cache/
.ipynb_checkpoints/
.env

# Do not upload publication to the git
publication/

# Local-only working docs (RUNBOOK, MAINTENANCE_LOG). Keep local, never commit.
local-docs/

# Participant re-identification key. NEVER commit: this is the only file that
# maps participant IDs back to real people. Keep it with the IRB records.
private/

CLAUDE.md

# NOTE: this block differs from `main` on purpose — do not merge it across.
# `main` is the reproduction branch and ignores the paths listed below so they
# can never be re-added there. `draft` is the full working branch and DOES keep
# them (notebooks/, literature/, IRB/, and the planning/background docs), so the
# ignore rules are deliberately absent here.

# NOTE: this block differs from `main` on purpose - do not merge it across.
# `main` blocks the manuscript cross-check tooling and the reviewer guide so they
# cannot reach a journal submission. `draft` is the full working branch and DOES
# keep them, so those ignore rules are deliberately absent here.

# Superseded combined instrument. Reviewers were given the balanced A/B/C sets,
# never this single-file version; build_review_questionnaire.py still emits it, so
# it is ignored rather than tracked to keep the tree clean.
data/review/aim3_accuracy_questionnaire.html

# Raw captures and derived artifacts (regenerable + potentially large)
# data/raw/
# data/cleaned/
# data/rewrites/
# data/scores/
# data/review/

# But keep the manifest checked in
!data/manifest.csv
!data/urls.csv

# Macroses
.DS_Store
.vscode/
.idea/
.claude/settings.local.json
