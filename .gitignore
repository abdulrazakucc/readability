.venv/
__pycache__/
*.pyc
.pytest_cache/
.ipynb_checkpoints/
.env

# Do not upload publication to the git
publication/

# Local-only working docs (RUNBOOK, MAINTENANCE_LOG). Keep local, never commit.
local-docs/

# Participant re-identification key. NEVER commit: this is the only file that
# maps participant IDs back to real people. Keep it with the IRB records.
private/

CLAUDE.md

# Kept on the `draft` branch only — never re-add these to `main`.
# `main` is the reproduction branch: code, locked config, data, reports and the
# protocol docs that govern the analysis. Background reading, internal planning
# and budgeting notes, the IRB package, and exploratory notebooks stay on `draft`.
notebooks/
literature/
IRB/
docs/cardiac_readability_plan.docx
docs/data_scientist_tasks.md
docs/jama_publishability_and_improvements.md
docs/cost_estimates.md
docs/background_summary.md
docs/literature_review.md

# Manuscript cross-check tooling and the reviewer guide live on `draft` only.
# `main` is the journal reproduction package: it must not carry the manuscript,
# anything parsed from it, or any file naming a participant.
docs/reviewer_guide_*.md
src/manuscript.py
scripts/14_validate_manuscript.py
scripts/15_comparison_report.py
tests/test_manuscript.py
reports/manuscript_validation.csv
reports/comparison_report.html

# Superseded combined instrument. Reviewers were given the balanced A/B/C sets,
# never this single-file version; build_review_questionnaire.py still emits it, so
# it is ignored rather than tracked to keep the tree clean.
data/review/aim3_accuracy_questionnaire.html

# Raw captures and derived artifacts (regenerable + potentially large)
# data/raw/
# data/cleaned/
# data/rewrites/
# data/scores/
# data/review/

# But keep the manifest checked in
!data/manifest.csv
!data/urls.csv

# Macroses
.DS_Store
.vscode/
.idea/
.claude/settings.local.json
